# Architecture

Lingui PO files are the runtime catalog. Tolgee is the review and context workspace. Google Cloud produces review-only candidates. Locale changes activate a catalog and remount the locale surface without reloading the browser.
