# String inventory

Inventory every visible string, accessibility attribute, metadata value, validation message, modal, and dynamic project field. A visible string is either a Lingui message or has an explicit, documented technical exemption.
