# Message contract

Use stable explicit IDs such as `contact.channel.email.detail`. Add extracted comments with route, UI surface, intended tone, and placeholder meaning. Never reuse a key merely because two English words happen to match.
