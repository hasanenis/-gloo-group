# Tolgee workflow

Import and export PO catalogs only. Attach route screenshots and descriptions, maintain the glossary, and export only `Reviewed` translations. Machine output is imported as a suggestion, never as an approved translation.
