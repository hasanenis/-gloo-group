# RTL and motion

Arabic uses RTL layout, Arabic-capable typography, zero letter spacing, and logical CSS properties. Animations must clean up before locale changes. Animated copy is a complete phrase in one bidi context, never an independently animated word plus a suffix.
