# Quality gates

CI rejects missing or unused keys, empty translations, mojibake, placeholder mismatches, unreviewed exports, and horizontal overflow. Locale switching is tested without a browser reload on every high-traffic route.
